apiVersion: v1
kind: Service
metadata:
  name: team-alpha-webapp
spec:
  ports:
  - nodePort: 30091
    port: 3000
    protocol: TCP
    targetPort: 80
  selector:
    app: team-alpha-webapp
  type: NodePort
